# 🚀 Быстрый запуск без базы данных

## Шаг 1: Установите Node.js

Скачайте и установите Node.js с официального сайта: https://nodejs.org/
(Выбирайте LTS версию - рекомендуемую для большинства пользователей)

## Шаг 2: Откройте терминал в папке проекта

**Windows:**
- Откройте папку с проектом в проводнике
- В адресной строке напишите `cmd` и нажмите Enter

**Mac/Linux:**
- Откройте терминал
- Перейдите в папку проекта командой `cd путь/к/проекту`

## Шаг 3: Установите зависимости

В терминале выполните:

```bash
npm install
```

Подождите 1-2 минуты, пока установятся все библиотеки.

## Шаг 4: Настройте порт (если 5000 занят)

Если порт 5000 уже занят другим приложением:

1. Скопируйте файл `.env.example` и назовите его `.env`
2. Откройте файл `.env` и измените строку `PORT=3000` на нужный вам порт

**Или просто создайте файл `.env` вручную** и напишите в нём:
```
PORT=3000
```

## Шаг 5: Запустите проект

```bash
npm run dev
```

## Шаг 6: Откройте в браузере

Откройте браузер и перейдите по адресу:

```
http://localhost:3000
```

(Если вы не меняли порт, используйте `http://localhost:5000`)

## ✅ Готово!

Лендинг запущен! Данные пользователей хранятся в памяти (после перезапуска сервера они удаляются).

---

## 📝 Если хотите сохранять данные

Если в будущем понадобится сохранять пользователей в базе данных:

1. Установите MySQL
2. Создайте файл `.env` в корне проекта
3. Добавьте строку:
   ```
   DATABASE_URL=mysql://root:пароль@localhost:3306/crypto_analyzer
   ```
4. Выполните `npm run db:push`
5. Перезапустите проект

---

## ❗ Решение проблем

### "npm: command not found"
**Проблема:** Node.js не установлен  
**Решение:** Установите Node.js с https://nodejs.org/

### "Port 5000 already in use"
**Проблема:** Порт 5000 занят другим приложением  
**Решение:** 
1. Скопируйте файл `.env.example` в `.env`
2. Откройте `.env` и измените `PORT=3000` (или любой другой свободный порт)
3. Перезапустите: `npm run dev`

### Ничего не работает
1. Убедитесь что Node.js установлен: `node --version`
2. Удалите папку `node_modules` и файл `package-lock.json`
3. Заново выполните `npm install`
4. Запустите `npm run dev`

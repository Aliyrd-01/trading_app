# Настройка автоматических сигналов

## ✅ Что было реализовано

1. **Расширение модели User** - добавлены поля для хранения настроек автоматических сигналов
2. **API endpoints** - созданы 3 новых endpoint:
   - `POST /api/auto_signals/settings` - сохранение настроек
   - `GET /api/auto_signals/settings` - получение настроек
   - `POST /api/auto_signals/test` - тестовая проверка сигналов
3. **Фоновая задача** - `auto_signals_worker.py` для автоматической проверки сигналов
4. **Миграция БД** - выполнена автоматически через `run_migration_auto_signals.py`

## 📋 Настройка автоматического запуска

### Windows (Task Scheduler)

1. Откройте **Планировщик заданий** (Task Scheduler)
2. Создайте новую задачу:
   - **Триггер**: Повторять каждые 5 минут
   - **Действие**: Запустить программу `start_auto_signals.bat`
   - **Рабочая папка**: путь к проекту `D:\trading\crypto-analyzer`

### Linux/Mac (Cron)

Выполните команду:
```bash
chmod +x setup_cron.sh
./setup_cron.sh
```

Или вручную добавьте в crontab:
```bash
*/5 * * * * cd /path/to/crypto-analyzer && python3 auto_signals_worker.py >> auto_signals.log 2>&1
```

## 🔧 Использование API

### Сохранение настроек автоматических сигналов

```javascript
POST /api/auto_signals/settings
{
  "enabled": true,
  "symbol": "BTC/USDT",
  "capital": 10000,
  "trading_type": "daytrading",
  "strategy": "balanced",
  "risk": 1.0,
  "confirmation": "ALL",
  "min_reliability": 60.0,
  "check_interval": 30
}
```

### Получение настроек

```javascript
GET /api/auto_signals/settings
```

### Тестовая проверка

```javascript
POST /api/auto_signals/test
```

## 📊 Интервалы проверки по типам торговли

- **Скальпинг**: каждые 5 минут
- **Дейтрейдинг**: каждые 30 минут
- **Свинг**: каждые 4 часа
- **Среднесрочная**: раз в день
- **Долгосрочная**: раз в день

## 🛡️ Защита от спама

Система автоматически пропускает повторные сигналы для той же валюты, если:
- Направление сигнала совпадает с предыдущим
- Цена изменилась менее чем на 1%

## 📝 Логирование

Все проверки и отправленные сигналы записываются в файл `auto_signals.log`

## ⚠️ Важные моменты

1. **Уведомления**: Для получения сигналов пользователь должен включить уведомления (email или Telegram) в настройках
2. **Минимальная надежность**: Сигналы отправляются только если `reliability_rating >= min_reliability`
3. **Обработка ошибок**: Если API биржи недоступен, проверка пропускается и повторяется при следующем запуске

## 🧪 Тестирование

1. Включите автоматические сигналы через API
2. Запустите тестовую проверку: `POST /api/auto_signals/test`
3. Проверьте логи: `tail -f auto_signals.log` (Linux) или откройте `auto_signals.log` (Windows)

## 📁 Созданные файлы

- `auto_signals_worker.py` - фоновая задача для проверки сигналов
- `run_migration_auto_signals.py` - скрипт миграции БД
- `migration_add_auto_signals.sql` - SQL миграция (альтернативный способ)
- `start_auto_signals.bat` - скрипт запуска для Windows
- `setup_cron.sh` - скрипт настройки cron для Linux
- `AUTO_SIGNALS_SETUP.md` - эта инструкция




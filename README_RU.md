# Crypto Analyzer - Инструкция по запуску

## 📋 Требования

Перед началом убедитесь, что у вас установлено:

- **Node.js** версии 18 или выше ([скачать](https://nodejs.org/))
- **npm** (устанавливается вместе с Node.js)
- **MySQL** версии 8.0 или выше ([скачать](https://dev.mysql.com/downloads/mysql/))

## 🚀 Пошаговая инструкция по запуску

### Шаг 1: Скачайте проект

```bash
# Если проект в Git репозитории
git clone <адрес-репозитория>
cd crypto-analyzer

# Или просто распакуйте архив и перейдите в папку проекта
```

### Шаг 2: Установите зависимости

```bash
npm install
```

Это установит все необходимые библиотеки (может занять 1-2 минуты).

### Шаг 3: Настройте базу данных MySQL

#### 3.1 Запустите MySQL

Убедитесь, что MySQL запущен на вашем компьютере.

#### 3.2 Создайте базу данных

Откройте терминал MySQL или используйте MySQL Workbench и выполните:

```sql
CREATE DATABASE crypto_analyzer CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### Шаг 4: Настройте переменные окружения

Создайте файл `.env` в корневой папке проекта:

```bash
# На Windows:
type nul > .env

# На Mac/Linux:
touch .env
```

Откройте файл `.env` в текстовом редакторе и добавьте:

```env
# Адрес базы данных MySQL
DATABASE_URL=mysql://root:ваш_пароль@localhost:3306/crypto_analyzer

# Секретный ключ для сессий (можно использовать любую случайную строку)
SESSION_SECRET=ваш_секретный_ключ_минимум_32_символа

# Окружение
NODE_ENV=development

# Порт (по умолчанию 5000)
PORT=5000
```

**Важно:** Замените `ваш_пароль` на пароль от вашей MySQL.

### Шаг 5: Обновите конфигурацию Drizzle

Откройте файл `drizzle.config.ts` и убедитесь, что там указан MySQL:

```typescript
export default defineConfig({
  out: "./migrations",
  schema: "./shared/schema.ts",
  dialect: "mysql",  // <-- Должно быть "mysql"
  dbCredentials: {
    url: process.env.DATABASE_URL,
  },
});
```

### Шаг 6: Создайте таблицы в базе данных

```bash
npm run db:push
```

Эта команда создаст необходимые таблицы в вашей базе данных.

### Шаг 7: Запустите проект

```bash
npm run dev
```

После запуска вы увидите сообщение:

```
serving on port 5000
```

### Шаг 8: Откройте приложение

Откройте браузер и перейдите по адресу:

```
http://localhost:5000
```

## 🎉 Готово!

Теперь вы можете:
- Зарегистрироваться на сайте
- Войти в систему
- Использовать функции анализатора криптовалют

## 🛠️ Полезные команды

```bash
# Запуск в режиме разработки (с автоперезагрузкой)
npm run dev

# Сборка проекта для production
npm run build

# Запуск production версии
npm run start

# Проверка типов TypeScript
npm run check

# Обновление схемы базы данных
npm run db:push
```

## ❓ Решение проблем

### Ошибка "DATABASE_URL must be set"

**Решение:** Убедитесь, что файл `.env` создан и содержит правильный `DATABASE_URL`.

### Ошибка подключения к MySQL

**Решение:** 
1. Проверьте, что MySQL запущен
2. Проверьте правильность пароля в `DATABASE_URL`
3. Убедитесь, что база данных `crypto_analyzer` создана

### Порт 5000 уже занят

**Решение:** Измените `PORT` в файле `.env` на другой (например, 3000 или 8080).

### Ошибка "tsx: not found"

**Решение:** Выполните `npm install` еще раз.

## 📁 Структура проекта

```
crypto-analyzer/
├── client/          # Фронтенд (React + TypeScript)
├── server/          # Бэкенд (Express + TypeScript)
├── shared/          # Общий код (схемы, типы)
├── attached_assets/ # Изображения и медиа
├── migrations/      # Миграции базы данных
└── .env            # Переменные окружения (создайте сами)
```

## 🔐 Безопасность

**Важно:** Файл `.env` содержит конфиденциальные данные. Никогда не загружайте его в Git или публичные репозитории!

## 💡 Дополнительная информация

- Приложение использует MySQL для хранения данных
- Фронтенд построен на React с Tailwind CSS
- Бэкенд использует Express.js
- Аутентификация через сессии с bcrypt для хеширования паролей

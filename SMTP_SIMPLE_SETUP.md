# Упрощенная настройка SMTP Email уведомлений

## 🔧 Проблема
SMTP настройки через переменные окружения могут не работать. Вот упрощенные варианты.

## ✅ Вариант 1: Прямая настройка в коде (для тестирования)

Откройте файл `app.py` и найдите строки 37-41. Замените их на:

```python
# SMTP настройки для Email
SMTP_HOST = "smtp.gmail.com"
SMTP_PORT = 587
SMTP_USER = "ваш_email@gmail.com"  # ← Вставьте ваш Gmail
SMTP_PASSWORD = "ваш_пароль_приложения"  # ← Вставьте пароль приложения
```

**Важно:** Используйте **пароль приложения**, а не обычный пароль Gmail!

### Как получить пароль приложения Gmail:
1. Включите двухфакторную аутентификацию в Google аккаунте
2. Перейдите: https://myaccount.google.com/apppasswords
3. Выберите "Почта" и "Другое устройство"
4. Введите название: "Crypto Analyzer"
5. Скопируйте 16-значный пароль (без пробелов)

---

## ✅ Вариант 2: Использовать только Telegram (рекомендуется)

Если SMTP вызывает проблемы, просто **отключите Email уведомления** и используйте только Telegram:

1. В интерфейсе снимите галочку "📧 Email уведомления"
2. Оставьте только "📱 Telegram уведомления"
3. Telegram работает стабильнее и проще в настройке

---

## ✅ Вариант 3: Использование .env файла (рекомендуется)

1. **Установите python-dotenv:**
   ```bash
   pip install python-dotenv
   ```

2. **Создайте файл `.env` в корне проекта** (скопируйте из `.env.example`)

3. **Заполните настройки:**
   ```
   SMTP_HOST=smtp.gmail.com
   SMTP_PORT=587
   SMTP_USER=your_email@gmail.com
   SMTP_PASSWORD=your_app_password
   TELEGRAM_BOT_TOKEN=your_bot_token
   ```

4. **Перезапустите приложение**

Приложение автоматически загрузит настройки из `.env` файла.

---

## ✅ Вариант 4: Альтернативные SMTP сервисы (международные)

### A. SendGrid (бесплатно до 100 писем/день)
1. Зарегистрируйтесь на https://sendgrid.com
2. Создайте API ключ
3. В `app.py`:
```python
SMTP_HOST = "smtp.sendgrid.net"
SMTP_PORT = 587
SMTP_USER = "apikey"  # Всегда "apikey"
SMTP_PASSWORD = "ваш_api_ключ_sendgrid"
```

### B. Mailgun (бесплатно до 5000 писем/месяц)
1. Зарегистрируйтесь на https://www.mailgun.com
2. Получите SMTP credentials
3. В `app.py`:
```python
SMTP_HOST = "smtp.mailgun.org"
SMTP_PORT = 587
SMTP_USER = "ваш_mailgun_username"
SMTP_PASSWORD = "ваш_mailgun_password"
```

### C. AWS SES (очень дешево, ~$0.10 за 1000 писем)
1. Зарегистрируйтесь на AWS
2. Настройте SES
3. В `app.py`:
```python
SMTP_HOST = "email-smtp.us-east-1.amazonaws.com"  # или ваш регион
SMTP_PORT = 587
SMTP_USER = "ваш_aws_access_key"
SMTP_PASSWORD = "ваш_aws_secret_key"
```

---

## ✅ Вариант 4: Файл конфигурации (безопаснее)

Создайте файл `config.py` в корне проекта:

```python
# config.py
SMTP_CONFIG = {
    "host": "smtp.gmail.com",
    "port": 587,
    "user": "ваш_email@gmail.com",
    "password": "ваш_пароль_приложения"
}
```

В `app.py` добавьте в начало:
```python
try:
    from config import SMTP_CONFIG
    SMTP_HOST = SMTP_CONFIG["host"]
    SMTP_PORT = SMTP_CONFIG["port"]
    SMTP_USER = SMTP_CONFIG["user"]
    SMTP_PASSWORD = SMTP_CONFIG["password"]
except ImportError:
    # Fallback на переменные окружения
    SMTP_HOST = os.getenv("SMTP_HOST", "smtp.gmail.com")
    SMTP_PORT = int(os.getenv("SMTP_PORT", "587"))
    SMTP_USER = os.getenv("SMTP_USER", "")
    SMTP_PASSWORD = os.getenv("SMTP_PASSWORD", "")
```

**Важно:** Добавьте `config.py` в `.gitignore`, чтобы не закоммитить пароли!

---

## 🐛 Отладка

Если письма не отправляются, проверьте логи в консоли:

```
📧 Попытка отправить Email уведомление на user@example.com (SMTP_USER: ваш_email@gmail.com)
📧 Подключение к SMTP серверу: smtp.gmail.com:587
✅ Email успешно отправлен с ваш_email@gmail.com на user@example.com
```

Если видите ошибки:
- `535 Authentication failed` → Неправильный пароль приложения
- `Connection refused` → Проблемы с сетью/файрволом
- `SMTP_USER=False` → Переменные окружения не установлены

---

## 💡 Рекомендация

**Для начала:** Используйте Вариант 1 (прямая настройка в коде) для тестирования.

**Для production:** Используйте Вариант 4 (config.py) или только Telegram уведомления.


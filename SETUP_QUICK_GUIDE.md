# 🚀 Быстрая инструкция по настройке

## 📋 Кто что настраивает?

### 🔧 Администратор (разово для всего сервера):

**1. Установка библиотеки:**
```bash
pip install requests
```
Это нужно сделать **один раз** на сервере.

**2. Настройка переменных окружения (разово):**

Создайте файл `.env` или установите переменные окружения:

```bash
# Telegram Bot Token (один бот для всех пользователей)
TELEGRAM_BOT_TOKEN="ваш_токен_от_BotFather"

# SMTP настройки (один сервер для всех)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="ваш_email@gmail.com"
SMTP_PASSWORD="пароль_приложения"
```

**Эти настройки применяются ко всем пользователям системы.**

---

### 👤 Пользователь (индивидуально в интерфейсе):

Каждый пользователь настраивает **свои** уведомления:

1. Откройте "⚙️ Продвинутые настройки"
2. Включите "🔔 Включить уведомления"
3. Настройте:
   - **Telegram Chat ID** (получите у @userinfobot)
   - Включите/выключите Telegram уведомления
   - Включите/выключите Email уведомления
   - Минимальный рейтинг для уведомлений

**✅ Настройки сохраняются автоматически в ваш профиль** и используются при каждом анализе.

**💡 После первой настройки вам не нужно каждый раз вводить Chat ID** — он сохраняется в вашем профиле.

---

## 📝 Итого:

| Что | Кто настраивает | Как часто |
|-----|----------------|-----------|
| `pip install requests` | Администратор | Разово |
| `TELEGRAM_BOT_TOKEN` | Администратор | Разово |
| `SMTP_*` настройки | Администратор | Разово |
| Telegram Chat ID | Пользователь | Один раз (сохраняется) |
| Включение уведомлений | Пользователь | По желанию |
| Минимальный рейтинг | Пользователь | По желанию |

---

## 🎯 Для администратора (шаги):

1. ✅ `pip install requests`
2. ✅ Создать Telegram бота у @BotFather
3. ✅ Получить Bot Token
4. ✅ Настроить SMTP (для Gmail нужен "Пароль приложения")
5. ✅ Установить переменные окружения

**Готово!** Теперь все пользователи могут настраивать свои уведомления в интерфейсе.

---

## 🎯 Для пользователя (шаги):

1. ✅ Войти в систему
2. ✅ Открыть "⚙️ Продвинутые настройки"
3. ✅ Включить "🔔 Включить уведомления"
4. ✅ Получить Chat ID у @userinfobot
5. ✅ Ввести Chat ID и включить Telegram/Email
6. ✅ Установить минимальный рейтинг

**Готово!** Настройки сохраняются автоматически. При следующем анализе уведомления будут отправляться автоматически (если рейтинг >= минимального).


























# Порт для запуска приложения (по умолчанию 5000)
# Если порт 5000 занят, измените на любой свободный, например 3000
PORT=3000

# Конфигурация базы данных MySQL (НЕОБЯЗАТЕЛЬНО)
# Если не указано, данные хранятся в памяти
# DATABASE_URL=mysql://root:password@localhost:3306/crypto_analyzer

# Секретный ключ для сессий (НЕОБЯЗАТЕЛЬНО)
# SESSION_SECRET=your-secret-key-change-this-to-random-string

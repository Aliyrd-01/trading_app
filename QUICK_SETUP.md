# ⚡ Быстрая настройка уведомлений

## 📝 Шаг 1: Откройте файл `config_notifications.py`

Этот файл находится в корне проекта. Просто откройте его в любом текстовом редакторе.

## 🤖 Шаг 2: Настройте Telegram (опционально)

**⚠️ Важно:** Telegram требует использования бота для отправки уведомлений. Это стандартный способ работы Telegram API. Пользователь должен отправить `/start` боту один раз, после этого все уведомления будут приходить автоматически.

1. **Создайте бота:**
   - Откройте Telegram
   - Найдите @BotFather
   - Отправьте `/newbot`
   - Следуйте инструкциям
   - Скопируйте полученный токен

2. **Вставьте токен в `config_notifications.py`:**
   ```python
   TELEGRAM_BOT_TOKEN = "ваш_токен_здесь"
   ```

3. **Сообщите пользователям имя бота** - они должны найти его в Telegram и отправить `/start`

## 📧 Шаг 3: Настройте Email (опционально)

### Для Gmail:

1. **Получите пароль приложения:**
   - Перейдите: https://myaccount.google.com/apppasswords
   - Выберите "Почта" и "Другое устройство"
   - Введите название: "Crypto Analyzer"
   - Скопируйте 16-символьный пароль (без пробелов!)

2. **Заполните в `config_notifications.py`:**
   ```python
   SMTP_HOST = "smtp.gmail.com"
   SMTP_PORT = 587
   SMTP_USER = "ваш_email@gmail.com"  # Ваш Gmail адрес
   SMTP_PASSWORD = "abcd efgh ijkl mnop"  # Пароль приложения (16 символов, можно с пробелами)
   ```

### Для Outlook/Hotmail:

```python
SMTP_HOST = "smtp-mail.outlook.com"
SMTP_PORT = 587
SMTP_USER = "ваш_email@outlook.com"
SMTP_PASSWORD = "ваш_пароль"
```

### Для Yahoo:

```python
SMTP_HOST = "smtp.mail.yahoo.com"
SMTP_PORT = 587
SMTP_USER = "ваш_email@yahoo.com"
SMTP_PASSWORD = "ваш_пароль_приложения"
```

## ✅ Шаг 4: Перезапустите приложение

После изменения `config_notifications.py` перезапустите приложение:
```bash
python desktop_app.py
```

## 🔍 Проверка работы

1. **Запустите анализ** с включенными уведомлениями
2. **Проверьте консоль** - там будут сообщения о попытке отправки
3. **Если есть ошибки** - они будут подробно описаны в консоли

## ❓ Проблемы?

### Telegram не работает:

**Ошибка "user not found" или "chat not found":**
1. ✅ **Пользователь должен начать диалог с ботом:**
   - Найдите вашего бота в Telegram (по имени, которое вы указали при создании)
   - Отправьте боту команду `/start`
   - Только после этого бот сможет отправлять уведомления

2. ✅ Проверьте, что `TELEGRAM_BOT_TOKEN` заполнен в `config_notifications.py`

3. ✅ Убедитесь, что пользователь указал правильный Telegram Username (без @ или с @)

4. ✅ Проверьте, что пользователь не заблокировал бота

5. ✅ Убедитесь, что у пользователя установлен Username в настройках Telegram (Настройки → Username)

6. ✅ Проверьте логи в консоли - там будет подробная информация об ошибке

### Email не работает:
- ✅ Для Gmail используйте **пароль приложения**, не обычный пароль!
- ✅ Убедитесь, что `SMTP_USER` совпадает с email аккаунта
- ✅ Проверьте логи в консоли - там будет указана точная причина ошибки

## 📚 Подробная документация

См. `USER_GUIDE.md` для полного руководства по всем функциям приложения.

